import React from 'react'
import ReactDOM from 'react-dom/client'
import { TeamSubmit } from './TeamSubmit.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TeamSubmit />
  </React.StrictMode>,
)
